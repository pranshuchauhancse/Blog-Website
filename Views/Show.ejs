<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= post.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <a href="/" class="btn btn-secondary mb-4">← Back to Home</a>

    <div class="card shadow p-4">
      <% if (post.image) { %>
        <img src="<%= post.image %>" class="card-img-top mb-4" style="max-height: 400px; object-fit: cover;">
      <% } %>
      <h2><%= post.title %></h2>
      <p class="text-muted"><%= post.description %></p>
      <hr />
      <div>
        <!-- Full content area (if you want to add later) -->
        <p>This is the full blog post view. You can add more details like content, author, etc.</p>
      </div>

      <div class="mt-4">
        <a href="/posts/<%= post._id %>/edit" class="btn btn-warning me-2">Edit</a>
        <form action="/posts/<%= post._id %>?_method=DELETE" method="POST" style="display:inline;" onsubmit="return confirm('Delete this post?');">
          <button class="btn btn-danger">Delete</button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
