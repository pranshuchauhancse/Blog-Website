<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Post</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h1 class="text-center mb-4">✏️ Edit Post</h1>

    <form action="/posts/<%= post._id %>?_method=PUT" method="POST">
      <div class="mb-3">
        <input type="text" name="title" class="form-control" placeholder="Title" value="<%= post.title %>" required />
      </div>
      <div class="mb-3">
        <input type="text" name="author" class="form-control" placeholder="Author Name" value="<%= post.author %>" required />
      </div>
      <div class="mb-3">
        <input type="text" name="image" class="form-control" placeholder="Image URL" value="<%= post.image %>" required />
      </div>
      <div class="mb-3">
        <textarea name="description" class="form-control" placeholder="Description" required><%= post.description %></textarea>
      </div>
      <div class="mb-3">
        <input type="date" name="date" class="form-control" value="<%= post.date %>" required />
      </div>
      <div class="mb-3">
        <input type="text" name="category" class="form-control" placeholder="Category / Tag (e.g. Tech, Travel)" value="<%= post.category %>" required />
      </div>
      <div class="d-grid gap-2">
        <button type="submit" class="btn btn-primary">Update Post</button>
      </div>
    </form>

    <div class="text-center mt-3">
      <a href="/" class="btn btn-secondary">← Back</a>
    </div>
  </div>
</body>
</html>
